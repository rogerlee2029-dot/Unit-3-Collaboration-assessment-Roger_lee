# Short Response Questinos
## 1. If you are collaborating with a classmate, what is the first thing that each of the team members needs to do? Why is it important to do this step?
The first thing that you need to do is to invite the person in github gui. This step is very important because if you do not invite the person that you want to add, he wont be able to make changes to what you did or to add their work to yours. For example, if I added somone into my work, they would be able to modify, add, and remove some part of my work, if I dont add them, they then will not be able to ake changes into my work.
## 2. Why do developers use branches ?
Developers use branches to make temprorary saves before putting it into the main files. Branches can be used for making new files, being for experiments, and fixing changes that someone made. Some examples that people use in branches is when someone wants to fix an error about something, which is named "bugfix/fix-typos-name".
## 3. What is git ? What is github? How does git and github work together?
Git are commands that are used by git developers to make files for other collaborators to read. Github is a website that git collaborators use in order to be able to put git commands into. These two work together by using git to put the commands into github in order to make a files that shows what you want to say. An example of them working together is when a person uses git commands in github to make work for people to see.
## 4. What is wrong with the following command sequense? What should be the correct command sequence? 
```
git commit -m "saving work"
git add . 
git push
```
 The main error that the person does in these lines of commands is that he used git commit before using git add, which makes nothing to save in the first place, the rewritten line of code is this:
 ```
git add .
git commmit -m "saving work"
git push
```